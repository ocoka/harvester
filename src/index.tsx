import * as React from 'react';
import * as ReactDOM from 'react-dom';
window.reactorTrace = false;
import { CoAppMemo } from './components/app';
ReactDOM.render(<CoAppMemo />, document.getElementById('app'));
